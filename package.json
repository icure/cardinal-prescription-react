{
  "name": "cardinal-prescription-react-lorka",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @icure/cardinal-prescription-be-react build",
    "start": "yarn workspace demo-app start"
  },
  "devDependencies": {
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-prettier": "^5.5.1",
    "prettier": "^3.6.2"
  }
}
